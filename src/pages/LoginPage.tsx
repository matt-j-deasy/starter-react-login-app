import { Flex } from "@chakra-ui/react";
import LoginForm from "components/LoginForm";

const LoginPage = () => {
  return (
    <>
      <Flex>
        <LoginForm />
      </Flex>
    </>
  );
};

export default LoginPage;
